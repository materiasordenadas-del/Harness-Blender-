# Curva a malla

Convierte una curva a malla solo cuando el usuario o la siguiente técnica lo
requiera. Antes de hacerlo, inspecciona la curva y valida radio, bevel y
resolución; después usa `validate_mesh`. La conversión deja de ser editable como
curva, aunque puede recuperarse mediante Undo inmediato del harness.
