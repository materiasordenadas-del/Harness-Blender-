# Curvas suaves

Para una transición orgánica, comienza con handles `AUTO` o `ALIGNED`. Usa
`VECTOR` solo si se desea un quiebre. Cambia un handle concreto antes de cambiar
toda la curva y vuelve a inspeccionar la trayectoria después de cada ajuste.
